import { InputBagSizeDirective } from './input-bag-size.directive';

describe('InputBagSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new InputBagSizeDirective();
    expect(directive).toBeTruthy();
  });
});
